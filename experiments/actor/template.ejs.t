---
to: src/components/<%= name %>.js
---

import React, { useState } from 'react';

const <%= name %> = (props) => {
  const [state, setState] = useState();

  return (
    <div>
      {/* Component code here */}
    </div>
  );
};

export default <%= name %>;